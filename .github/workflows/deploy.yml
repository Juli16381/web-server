name: Deploy Python Sniffer and Node.js 

on:
  push:
    branches:
      - main

jobs:
  deploy-on-juli16381:
    runs-on: [self-hosted, juli16381]  # Ejecutar en el runner "juli16381"
    steps:
    - uses: actions/checkout@v2
    
    # Configurar el entorno de Python
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: pip install -r requirements.txt

    - name: Run Python Sniffer
      run: |
        nohup python Sniffer_prueba.py &

    # Configurar el entorno de Node.js
    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'
        
    - name: Install Node.js dependencies
      run: npm install

    - name: Start Node.js server
      run: |
        nohup node server.js &

  deploy-on-mlcuentas:
    runs-on: [self-hosted, mlcuentas]  # Ejecutar en el runner "mlcuentas"
    steps:
    - uses: actions/checkout@v2

    # Configurar el entorno de Python
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: pip install -r requirements.txt

    - name: Run Python Sniffer
      run: |
        nohup python Sniffer_prueba.py &

    # Configurar el entorno de Node.js 
    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'
        
    - name: Install Node.js dependencies
      run: npm install

    - name: Start Node.js server
      run: |
        nohup node server.js &

