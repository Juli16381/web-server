name: Deploy Python Sniffer and Node.js

on:
  push:
    branches:
      - main

jobs:
  deploy-on-juli16381:
    runs-on: [self-hosted, juli16381]  # Ejecutar en el runner "juli16381"
    steps:
    - uses: actions/checkout@v2
    - name: Checkout code
      run: echo "it works on juli16381"

  deploy-on-mlcuentas:
    runs-on: [self-hosted, mlcuentas]  # Ejecutar en el runner "mlcuentas"
    steps:
    - uses: actions/checkout@v2
    - name: Checkout code
      run: echo "it works on mlcuentas"



