name: Deploy Python Sniffer and Node.js

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: juli16381

    steps:
    - uses: actions/checkout@v2
    - name: Checkout code
      run: echo "it works"



